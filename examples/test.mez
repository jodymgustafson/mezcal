function rFactorial(n)
begin
  if n < 0 then error "Invalid value" 
  if n == 0 then return 1
  return n * rFactorial(n - 1)
end

function factorial(n)
begin
  if n < 0 then error "Invalid value" 
  let result = 1;
  while n > 1
    result = result * n
    n = n - 1
  end
  return result
end

# Recursive fibonacci 
function rFibonacci(n)
begin
  if n <= 1 then return n
  return rFibonacci(n - 2) + rFibonacci(n - 1)
end

# Converts celsius to fahrenheit
function celsius2Fahr(c) begin
  return (c * 9) / 5 + 32
end

# Converts fahrenheit to celsius
function fahr2Celsius(f) begin
 return (f - 32) * 5 / 9
end

let a = input("Convert from fahrenheit or celsius (f/c)")
if a == "c" then begin
  c = input("Enter degrees celsius: ")
  print(c ," celsius is ", fahr2Celsius(c))
end
if a == "f" then begin
  f = input("Enter degrees fahrenheit: ")
  print(f ," fahrenheit is ", celsius2Fahr(f))
end
